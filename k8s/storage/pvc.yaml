apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-backend-data-pvc
  labels:
    app: uptime-monitor-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
